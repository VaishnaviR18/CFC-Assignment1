stage("build") {
    def a = "8.50.0.CK2";
    def productVersion;
        
    if(a.charAt(4) == '0') {
        productVersion = "fuse-" + a.substring(0, 3);
    } else{
        productVersion = "fuse-" + a.substring(0, 4);
    }

    echo "productVersion";
}